# Кэш интерпретатора. Пересоздаётся при каждом запуске генератора, поэтому
# в истории от него только шум: 14 .pyc меняли хеш на каждом прогоне и
# засоряли diff, в котором владелец смотрит правки страниц.
__pycache__/
*.py[cod]
