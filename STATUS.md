# STATUS — ретрофит под seo-2026-playbook

## kran365.ru — этап 1 (шелл/шаблон) готов, 25.09.2026

**Baseline (до патча):** 209 стр. проверено, 3 FAIL (yandex_*.html — не страницы, игнор), 206 WARN, 0 PASS.
Топ-проблем: нет автора (209), нет источников (208), H2 без id (206), сущность не названа (206), нет TOC (205), первое предложение не тезис (199), короткий intro (130), нет `<time>` (86), нет FAQ (62).

**Сделано:** новый модуль `tools/fix_geo2026.py`, подключён в `page()` рядом с `fix_schema.patch_html` (тот же паттерн, что уже был в кодовой базе). Патчит собранный HTML в памяти, не трогая ни одну из `*_prose`-функций:
- `id` на каждый `<h2>` (транслитерированный слаг, уникальность гарантирована).
- Автоматический `<nav class="toc">` при ≥3 H2, вставляется перед первым H2.
- Строка «Редакция КРАН365 · опубликовано DD.MM.YYYY [· обновлено DD.MM.YYYY]» — даты берутся из реальной `git log --follow` истории файла (не выдуманы); если файла ещё не было в git — только «опубликовано» сегодняшним числом.

**После патча:** 209 проверено, 3 FAIL (те же yandex_*.html), 206 WARN, 0 PASS.
- H2 без id: 206 → **0**
- нет TOC: 205 → **0**
- нет автора: 209 → **7** (страницы без `<p class="lead">` — регэксп их не поймал, разобрать отдельно)
- нет `<time datetime>`: 86 → **6**

Живая проверка: `curl` на https://kran365.ru/avtokrany/ и /blog/kak-vybrat-avtokran/ — обе строки (автор+даты, TOC) на месте после деплоя. 390px — без горизонтального скролла, meta-row переносится корректно.

Коммит `23eb692`, задеплоено на main → GitHub Pages.

**НЕ сделано в этом проходе (сознательно, это контент-уровень, не шаблон-уровень — требует переписывания текста в ~15 `*_prose`-функциях, не regex-патча):**
- Блок источников (208 стр.) — нет реальных ссылок на нормативку/производителей в большинстве prose-функций, добавлять по одной, не выдумывать.
- Сущность «КРАН365»/название техники в каждом абзаце (206) — нужно переписывать первые предложения секций.
- Первое предложение = тезис с фактом (199), длина intro 40-90 слов (130) — то же самое, переписывание prose.
- FAQ (62 стр., в основном хабы категорий и гео-страницы) — нужны реальные вопросы под спрос, не шаблонные.
- 7 страниц без автора и 6 без дат — найти и разобрать отдельно (не совпали с regex `<p class="lead">`).

**Дальше:** либо (а) точечно переписать 1-2 самые трафиковые prose-функции (`tonnage_prose`, `strela_prose`) под answer-unit как пилот, либо (б) перейти к шаблонному ретрофиту следующего сайта сети (moskran-servis.ru) тем же методом (общий шаблон, не постранично).

## Остальные 7 сайтов — baseline ещё не снят
moskran-servis.ru, manipmo.ru, kranneva.ru, kran-lo.ru, kran-kuban.ru, kran-rt.ru, stavki-rating.ru — очередь та же: geo_check.py → патч общего шаблона (для каждого сайта свой фреймворк — Astro/Nuxt/Next/11ty/SvelteKit/sitegen, техника патча та же идея, но код разный) → пересборка → повторная проверка → живая проверка → деплой → STATUS.md.
